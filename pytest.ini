[pytest]
# Default collection covers the active MacroEdge suite (tests/) and the retained
# Biotech Risk Scout suite (biotech-risk-scout/tests/). Archived pre-pivot tests
# under legacy/ (which need pandas/numpy) are intentionally excluded; run them
# from the legacy/ directory if needed.
testpaths = tests biotech-risk-scout/tests
